export const config = { defaultDelay: 0, defaultTyping: 0 }

import React from 'react';
import ReactDOM from 'react-dom';
import App from './webchat/index';

ReactDOM.render(<App />, document.getElementById('root'));